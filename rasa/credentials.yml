# This file contains the credentials for the voice & chat platforms
# which your bot is using.
# https://rasa.com/docs/rasa/messaging-and-voice-channels

# Enable REST for interaction with a frontend or UI
rest:

# MongoDB credentials for persistent storage (if required for Rasa tracker or other data)
# Uncomment and configure these if you want to use MongoDB to store conversation data
# tracker_store:
#   type: mongod
#   url: mongodb+srv://<username>:<password>@cluster.mongodb.net/dbname
#   db: <database name>
#   username: <username>
#   password: <password>
#   auth_source: admin

# OpenAI API for integrations, using environment variable for security
openai:
  api_key: "${OPENAI_API_KEY}"

# Example integrations for other channels (uncomment and configure as needed)

#facebook:
#  verify: "<verify>"
#  secret: "<your secret>"
#  page-access-token: "<your page access token>"

#slack:
#  slack_token: "<your slack token>"
#  slack_channel: "<the slack channel>"
#  slack_signing_secret: "<your slack signing secret>"

#socketio:
#  user_message_evt: "user_uttered"
#  bot_message_evt: "bot_uttered"
#  session_persistence: true

#mattermost:
#  url: "https://<mattermost instance>/api/v4"
#  token: "<bot token>"
#  webhook_url: "<callback URL>"

# Rasa Enterprise (if applicable)
# rasa:
#   url: "http://localhost:5002/api"
